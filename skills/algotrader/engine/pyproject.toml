[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "algotrader-engine"
requires-python = ">=3.11"
description = "PyO3 bridge for the algotrader signal processing pipeline"
version = "0.1.0"

[tool.maturin]
features = ["python"]
