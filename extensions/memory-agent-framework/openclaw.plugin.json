{
  "id": "memory-agent-framework",
  "kind": "memory",
  "uiHints": {
    "cliBin": {
      "label": "CLI Binary Path",
      "placeholder": "agent",
      "help": "Absolute path to the agent-framework CLI binary (defaults to 'agent' on PATH)",
      "advanced": true
    },
    "apiUrl": {
      "label": "API URL",
      "placeholder": "http://webapp.agent-framework.orb.local",
      "help": "Base URL of your agent-framework instance"
    },
    "apiKey": {
      "label": "API Key",
      "sensitive": true,
      "placeholder": "af_...",
      "help": "Agent Framework API key (or use ${AF_API_KEY})"
    },
    "autoRecall": {
      "label": "Auto-Recall",
      "help": "Inject relevant memories into context before each agent response"
    },
    "autoCapture": {
      "label": "Auto-Capture",
      "help": "Ingest user messages into memory after each conversation ends"
    },
    "recallLimit": {
      "label": "Recall Limit",
      "help": "Maximum number of memories to inject per request",
      "advanced": true,
      "placeholder": "5"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "cliBin": { "type": "string" },
      "apiUrl": { "type": "string" },
      "apiKey": { "type": "string" },
      "autoRecall": { "type": "boolean" },
      "autoCapture": { "type": "boolean" },
      "recallLimit": { "type": "number", "minimum": 1, "maximum": 20 }
    },
    "required": ["apiUrl"]
  }
}
